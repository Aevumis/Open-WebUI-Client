// This stub is intentionally empty because the library ships its own types.
// Keeping an empty augmentation avoids TS errors in environments lacking types,
// but won't conflict with the package's declarations.
declare module 'react-native-markdown-display' {}
